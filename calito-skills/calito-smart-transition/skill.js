(()=>{
  const DATA=window.CALITO_OPERATIONAL_MEMORY||{};
  const ENDPOINT=String(window.CALITO_FORM_ENDPOINT||'');
  const API=ENDPOINT.replace(/\/api\/alinhamento\/?$/,'');
  const BASE=DATA.financeiro_transicao||{valor_total:200000,lancamentos:[]};
  const PENDING_KEY='calito:smart:pending:v3';
  const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const dateBR=s=>{if(!s)return '-';const [y,m,d]=String(s).slice(0,10).split('-');return `${d}/${m}/${y}`};
  const pending=()=>{try{return JSON.parse(localStorage.getItem(PENDING_KEY)||'[]')}catch{return []}};
  const savePending=v=>localStorage.setItem(PENDING_KEY,JSON.stringify(v));
  function summary(finance){const total=Number(finance.valor_total||200000);const list=finance.lancamentos||[];const pago=list.reduce((s,l)=>s+Number(l.valor||0),0);return {valor_total:total,pago,saldo:Math.max(0,total-pago),lancamentos:list,atualizado_em:finance.atualizado_em||null}}
  function mergedFallback(){const base=JSON.parse(JSON.stringify(BASE));base.lancamentos=base.lancamentos||[];const ids=new Set(base.lancamentos.map(x=>x.id));for(const p of pending())if(!ids.has(p.id))base.lancamentos.push({...p,pendente_sync:true});return summary(base)}
  function ensureStyles(){if(document.getElementById('smart-transition-style'))return;const s=document.createElement('style');s.id='smart-transition-style';s.textContent=`.smart-modal{position:fixed;inset:0;background:rgba(15,23,42,.65);display:none;align-items:flex-start;justify-content:center;padding:20px 10px;overflow:auto;z-index:1000}.smart-modal.open{display:flex}.smart-card{width:min(980px,100%);background:#fff;border-radius:24px;padding:22px;box-shadow:0 30px 80px rgba(0,0,0,.3)}.smart-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.smart-close{border:0;background:#eef2f7;border-radius:12px;padding:9px 12px;cursor:pointer}.smart-grid{display:grid;grid-template-columns:repeat(4,minmax(130px,1fr));gap:10px;margin:16px 0}.smart-kpi{border:1px solid #e5e7eb;border-radius:14px;padding:13px;background:#fffaf0}.smart-kpi small{display:block;color:#6b7280}.smart-kpi b{font-size:19px}.smart-progress{height:12px;background:#edf2f7;border-radius:999px;overflow:hidden;margin-bottom:18px}.smart-progress span{display:block;height:100%;background:linear-gradient(135deg,#fdc830,#f37335)}.smart-form{display:grid;grid-template-columns:160px 150px 1fr auto;gap:10px;align-items:end;padding:16px;border:1px solid #e5e7eb;border-radius:16px;background:#f8fafc}.smart-form label{display:block;font-size:12px;color:#64748b;margin-bottom:5px}.smart-form input{width:100%;min-height:44px;border:1px solid #d5d9e3;border-radius:10px;padding:8px 10px;font:inherit}.smart-form button{min-height:44px;border:0;border-radius:10px;background:linear-gradient(135deg,#fdc830,#f37335);color:#fff;font-weight:800;padding:0 16px;cursor:pointer}.smart-history{display:grid;gap:8px;margin-top:14px}.smart-entry{display:grid;grid-template-columns:100px 130px 1fr 150px;gap:10px;padding:12px;border:1px solid #e6e8ee;border-radius:13px;align-items:center}.smart-entry .amount{font-weight:900}.smart-entry .author{font-size:12px;color:#64748b}.smart-note{background:#fff7ed;border-left:4px solid #f37335;padding:12px 14px;border-radius:10px;line-height:1.5;margin-top:14px}.smart-status{font-size:13px;color:#475569;margin-top:10px}.smart-section{margin-top:22px}.smart-pending{color:#a16207;font-weight:700}@media(max-width:760px){.smart-grid{grid-template-columns:repeat(2,1fr)}.smart-form{grid-template-columns:1fr}.smart-entry{grid-template-columns:1fr 1fr}.smart-entry .desc,.smart-entry .author{grid-column:1/-1}}`;document.head.appendChild(s)}
  async function liveLedger(){if(!API)throw new Error('endpoint_missing');const controller=new AbortController();const t=setTimeout(()=>controller.abort(),12000);try{const r=await fetch(API+'/api/transicao-financeiro',{cache:'no-store',signal:controller.signal});if(!r.ok)throw new Error('load_failed');return await r.json()}finally{clearTimeout(t)}}
  async function syncPending(){if(!API)return;const list=pending();if(!list.length)return;const remaining=[];for(const item of list){try{const r=await fetch(API+'/api/transicao-financeiro',{method:'POST',headers:{'content-type':'application/json','x-idempotency-key':item.id},body:JSON.stringify({data:item.data,valor:item.valor,descricao:item.descricao,autor:'Rogger',tipo:item.tipo||'pagamento',id:item.id})});if(!r.ok)throw new Error()}catch{remaining.push(item)}}savePending(remaining)}
  async function submitEntry(ev){ev.preventDefault();const form=ev.currentTarget,status=document.getElementById('smart-status');const fd=new FormData(form);const item={id:'local-'+crypto.randomUUID(),data:fd.get('data'),valor:Number(fd.get('valor')),descricao:String(fd.get('descricao')||'').trim(),tipo:'pagamento',autor:'Rogger',registrado_em:new Date().toISOString()};if(!item.data||!item.valor||!item.descricao)return;status.textContent='Registrando…';try{if(!API)throw new Error();const r=await fetch(API+'/api/transicao-financeiro',{method:'POST',headers:{'content-type':'application/json','x-idempotency-key':item.id},body:JSON.stringify(item)});if(!r.ok)throw new Error();status.textContent='Lançamento salvo no histórico compartilhado.'}catch{const q=pending();q.push({...item,pendente_sync:true});savePending(q);status.textContent='Render indisponível agora. Lançamento guardado neste aparelho e será sincronizado automaticamente.'}form.reset();form.querySelector('[name=data]').value=new Date().toISOString().slice(0,10);await render()}
  function build(){ensureStyles();if(document.getElementById('smart-transition-modal'))return;const modal=document.createElement('div');modal.id='smart-transition-modal';modal.className='smart-modal';modal.innerHTML='<div class="smart-card"></div>';document.body.appendChild(modal);modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')})}
  async function render(){build();const card=document.querySelector('#smart-transition-modal .smart-card');card.innerHTML='<div class="smart-head"><div><h2>Calito Smart — Acerto</h2><p>Livro financeiro da negociação.</p></div><button class="smart-close">Fechar</button></div><div class="smart-note">Carregando histórico…</div>';card.querySelector('.smart-close').onclick=()=>document.getElementById('smart-transition-modal').classList.remove('open');await syncPending();let ledger,online=true;try{ledger=await liveLedger()}catch{ledger=mergedFallback();online=false}const local=pending();if(online&&local.length){ledger.lancamentos=[...(ledger.lancamentos||[]),...local];ledger=summary(ledger)}const pct=ledger.valor_total?Math.min(100,ledger.pago/ledger.valor_total*100):0;const entries=[...(ledger.lancamentos||[])].sort((a,b)=>String(b.data).localeCompare(String(a.data))||String(b.registrado_em||'').localeCompare(String(a.registrado_em||'')));card.innerHTML=`<div class="smart-head"><div><h2>Calito Smart — Acerto</h2><p>Registro dos pagamentos, créditos e mercadorias abatidos do valor total.</p></div><button class="smart-close">Fechar</button></div><div class="smart-grid"><div class="smart-kpi"><small>Valor total</small><b>${money(ledger.valor_total)}</b></div><div class="smart-kpi"><small>Total registrado</small><b>${money(ledger.pago)}</b></div><div class="smart-kpi"><small>Saldo restante</small><b>${money(ledger.saldo)}</b></div><div class="smart-kpi"><small>Quitação</small><b>${pct.toLocaleString('pt-BR',{maximumFractionDigits:1})}%</b></div></div><div class="smart-progress"><span style="width:${pct}%"></span></div>${online?'':'<div class="smart-note">O servidor está temporariamente indisponível. O histórico abaixo veio da última base publicada; novos lançamentos ficam salvos neste aparelho e sincronizam depois.</div>'}<div class="smart-section"><h3>Novo lançamento</h3><form class="smart-form" id="smart-form"><div><label>Data</label><input name="data" type="date" value="${new Date().toISOString().slice(0,10)}" required></div><div><label>Valor (R$)</label><input name="valor" type="number" min="0.01" step="0.01" required></div><div><label>Descrição</label><input name="descricao" maxlength="500" placeholder="PIX, boleto, mercadoria, cartão, crédito…" required></div><button type="submit">Registrar</button></form><div class="smart-status" id="smart-status">Editor deste módulo: <b>Rogger</b></div></div><div class="smart-section"><h3>Histórico</h3><div class="smart-history">${entries.map(e=>`<div class="smart-entry"><div>${dateBR(e.data)}</div><div class="amount">${money(e.valor)}</div><div class="desc">${esc(e.descricao)}</div><div class="author">${e.pendente_sync?'<span class="smart-pending">Pendente de sincronização</span><br>':''}Editado por: <b>${esc(e.autor||'registro')}</b>${e.registrado_em?'<br>'+new Date(e.registrado_em).toLocaleString('pt-BR'):''}</div></div>`).join('')||'<div class="smart-note">Nenhum lançamento registrado.</div>'}</div></div>`;card.querySelector('.smart-close').onclick=()=>document.getElementById('smart-transition-modal').classList.remove('open');card.querySelector('#smart-form').onsubmit=submitEntry}
  async function open(){build();document.getElementById('smart-transition-modal').classList.add('open');await render()}
  function addButton(){const quick=document.querySelector('.quick');if(!quick||document.getElementById('btn-smart-transition'))return;const b=document.createElement('button');b.id='btn-smart-transition';b.type='button';b.textContent='Calito Smart';b.onclick=open;quick.appendChild(b)}
  build();addButton();window.CALITO_SKILLS=window.CALITO_SKILLS||{};window.CALITO_SKILLS['calito-smart-transition']={version:'3.0.0',status:'ativo',modo:'acerto-financeiro-resiliente',open};
})();